<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetMealDishMapper">
    <insert id="saveBatch">
        insert into setmeal_dish
        (setmeal_id, dish_id, name, price, copies)
        values
        <foreach collection="setmealDishes" item="setmealDish" separator="," >
            (#{setmealDish.setmealId},#{setmealDish.dishId},#{setmealDish.name},#{setmealDish.price},#{setmealDish.copies})
        </foreach>

    </insert>
    <delete id="deleteBySetmealIds">
        delete from setmeal_dish
        where setmeal_id
        in
        <foreach collection="setmealIds" item="setmealId" separator="," open="(" close=")">
            #{setmealId}
        </foreach>
    </delete>


    <select id="findDishesByDishIds" resultType="java.lang.Long">
        select setmeal_id from setmeal_dish where dish_id in
        <foreach collection="ids" item="dishId" separator="," open="(" close=")">
            #{dishId}
        </foreach>
    </select>


    <select id="pageQuery" resultType="com.sky.vo.SetMealPageVO">
        select a.*,b.name from setmeal a left outer join category b on a.category_id=b.id
        <where>
            <if test="name != null and name != ''">
                and a.name like concat('%',#{name},'%')
            </if>
            <if test="status != null">
                and a.status like concat('%',#{status},'%')
            </if>
            <if test="categoryId != null">
                and a.category_id like concat('%',#{categoryId},'%')
            </if>
        </where>
        order by a.update_time desc
    </select>

</mapper>
